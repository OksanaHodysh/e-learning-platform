import { FilterAuthorsPipe } from './filter-authors.pipe';

describe('FilterAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
